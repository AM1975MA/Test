name: Titanium Source-Faithful Audit

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  corrected-fast:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    permissions:
      actions: read
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install path-analysis dependencies
        run: |
          python -m pip install --upgrade pip
          pip install numpy==2.3.5 pandas==2.2.3 pyarrow matplotlib
      - name: Download executed OOS model output
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          curl -L --fail -H "Authorization: Bearer ${GH_TOKEN}" -H "Accept: application/vnd.github+json" \
            https://api.github.com/repos/AM1975MA/Test/actions/artifacts/8839840705/zip -o prior_artifact.zip
          mkdir prior
          unzip -q prior_artifact.zip -d prior
          test -f prior/titanium_retrained_output/OOS_TICKER_SCORES.parquet
      - name: Reconstruct and verify vectorized corrected simulator
        run: |
          cat gzpayload/recompute_fast.b64 | base64 -d | gzip -dc > recompute_titanium_paths_fast.py
          echo '263e5d19da4525bff41b05f7c7f128e8ccedf11760a7f558eed55307924387bd  recompute_titanium_paths_fast.py' | sha256sum -c -
          python -m py_compile recompute_titanium_paths_fast.py
      - name: Recompute all 500 basket and Global150 paths
        run: python recompute_titanium_paths_fast.py
      - name: Archive corrected output
        if: always()
        run: zip -r TITANIUM_CORRECTED_FAST.zip titanium_corrected_fast || true
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: TITANIUM_CORRECTED_FAST
          path: |
            TITANIUM_CORRECTED_FAST.zip
            titanium_corrected_fast/
          retention-days: 7
