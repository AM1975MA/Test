name: Titanium Legacy Sensitivity Matrix
on:
  workflow_dispatch:
jobs:
  disabled-by-default:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy sensitivity matrix intentionally disabled for exact V23 regeneration.'
